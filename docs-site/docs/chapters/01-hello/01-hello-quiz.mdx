---
sidebar_position: 2
title: "Quiz: Hello wwwBank"
---

import Quiz from '@site/src/components/Quiz/Quiz';
import QuizQuestion from '@site/src/components/Quiz/QuizQuestion';

# Quiz: Hello wwwBank

<Quiz chapterId="ch01">
  <QuizQuestion
    question="Which Flutter widget provides a vertical navigation rail for desktop layouts?"
    options={[
      "BottomNavigationBar",
      "Drawer",
      "NavigationRail",
      "TabBar"
    ]}
    correctIndex={2}
    explanation="NavigationRail is designed for desktop and tablet layouts, providing vertical navigation alongside the main content."
  />
  <QuizQuestion
    question="What is the purpose of ShellRoute in go_router?"
    options={[
      "To add shell scripts to the routing configuration",
      "To wrap a group of child routes in a shared persistent outer widget",
      "To redirect unauthenticated users to the login page",
      "To define the initial route of the application"
    ]}
    correctIndex={1}
    explanation="ShellRoute wraps its child routes with a shared builder widget — in wwwBank, that's WwwBankScaffold. This means the navigation rail and header stay mounted while only the body slot changes as you navigate between routes."
  />
  <QuizQuestion
    question="In wwwBank, how does go_router know to re-run the auth redirect when the user logs in?"
    options={[
      "A Timer polls AppState every second",
      "The redirect function is called on every widget rebuild",
      "refreshListenable: appState tells go_router to re-evaluate redirects whenever AppState notifies",
      "LoginScreen calls GoRouter.of(context).refresh() manually"
    ]}
    correctIndex={2}
    explanation="Passing refreshListenable: appState to GoRouter links the router's redirect logic to AppState's ChangeNotifier. Every time appState.notifyListeners() fires — on login or logout — go_router re-runs the redirect function and updates the active route."
  />
  <QuizQuestion
    question="At what viewport width does WwwBankScaffold switch from BottomNavigationBar to NavigationRail?"
    options={[
      "400 px",
      "480 px",
      "600 px",
      "768 px"
    ]}
    correctIndex={2}
    explanation="WwwBankScaffold defines a _breakpoint constant of 600 px. When MediaQuery.sizeOf(context).width is 600 or greater, it renders NavigationRail inside a Row. Below 600 px, the body fills the full height and NavigationBar appears at the bottom."
  />
</Quiz>
